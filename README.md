# Ponent
ponent: official Posita compiler, translating source to native code via Cranelift with an optional strict mode for SMT‑based contract verification. Frontend development is active; a fully tested lexer is already in place.
